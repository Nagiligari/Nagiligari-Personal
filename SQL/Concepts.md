1. A database simply it is a container that stores data.
2. SQL is the language that you use in order to talk to the database.
3. Database management system is a software that can manage all the requestd to a database and its going to make the priority which SQL must be executed first.This software can as well manage the security wheather the SQL is allowed to be executed in the firstplace.
4. databases are classified into SQL and NO SQL databases
5. Relational database : contains data in the form of rows and columns(SQL database)
6. key-value: each key is mapped with one or more values 
7. column based: data is stored in form of columns 
8. graph : data is stored in the form of graphs 
9. documents: mongo DB
10. DDL: CREATE, ALTER, DROP
11. DML: INSERT,UPDATE,DELETE
12. Data Query Language: SELETE